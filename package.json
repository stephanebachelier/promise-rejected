{
  "name": "promise-rejected",
  "version": "0.0.0",
  "description": "Return rejected promises",
  "main": "index.js",
  "scripts": {
    "test": "standard && NODE_ENV=test ava test.js",
    "test-cov": "standard && NODE_ENV=test nyc ava test.js",
    "coverage": "standard && NODE_ENV=test nyc --reporter=html --reporter=text ava test.js"
  },
  "repository": "stephanebachelier/promise-rejected",
  "keywords": [
    "bluebird",
    "rejected",
    "native",
    "promise"
  ],
  "license": "MIT",
  "devDependencies": {
    "ava": "^0.18.2",
    "is-promise": "^2.1.0",
    "nyc": "^10.2.0",
    "standard": "^4.5.2"
  },
  "files": [
    "LICENSE",
    "README.md",
    "index.js"
  ]
}
